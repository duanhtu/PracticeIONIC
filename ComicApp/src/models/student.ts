export class Student {
    id: string
    name: string
    classRoom: string

    constructor(id , name, classRoom) {
        this.id = id;
        this.name = name;
        this.classRoom = classRoom;
    }
}

